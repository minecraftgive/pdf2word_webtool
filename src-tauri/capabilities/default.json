{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window: invoke commands and listen to events.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
